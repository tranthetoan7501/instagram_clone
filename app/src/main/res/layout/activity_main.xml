<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <RelativeLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        xmlns:tools="http://schemas.android.com/tools"
        android:id="@+id/container"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        >

        <FrameLayout
            android:id="@+id/appbar"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            >
            <include android:id="@+id/home_appbar" layout="@layout/appbar_home" android:visibility="invisible"
                />
            <include android:id="@+id/account_appbar" layout="@layout/appbar_account" android:visibility="invisible"
                />
            <include android:id="@+id/search_appbar" layout="@layout/appbar_search" android:visibility="invisible"
                />
            <include android:id="@+id/favorite_appbar" layout="@layout/appbar_favorite" android:visibility="invisible"
                />
        </FrameLayout>


        <FrameLayout
            android:layout_below="@+id/appbar"
            android:layout_above="@id/navigation"
            android:id="@+id/fragment_layout"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            app:layout_behavior="@string/appbar_scrolling_view_behavior" />


        <com.google.android.material.bottomnavigation.BottomNavigationView
            android:id="@+id/navigation"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_alignParentBottom="true"
            app:itemIconTint="@color/black"
            app:labelVisibilityMode="unlabeled"
            app:menu="@menu/bottom_navigation" />


    </RelativeLayout>
     <include
        layout="@layout/up_item_fragment"
        />
</androidx.coordinatorlayout.widget.CoordinatorLayout>
