package com.hcmus.mobilappsocialnetworkingimage.photoEditor.filters;

import com.example.photoeditor.PhotoFilter;

public interface FilterListener {
    void onFilterSelected(PhotoFilter photoFilter);
}